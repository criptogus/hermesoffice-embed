# Catalog entry for hermesoffice-embed.
#
# Submit as a PR adding this file to NousResearch/hermes-agent at
# `plugin-catalog/hermesoffice-embed.yaml`.
#
# Checklist (website/docs/user-guide/features/plugin-catalog.md → "Submitting
# a plugin to the catalog"):
#   [x] 1. Owner-submitted  — the repo belongs to the submitting account
#   [x] 2. Public repository
#   [x] 3. Released — v0.1.1 (tag + GitHub release). v0.1.0 is flagged
#          pre-release because it declares manifest_version 2 and cannot install.
#   [ ] 4. Catalog validation workflow green on the PR
#   [ ] 5. Pinned SHA >= 2 weeks old
#          committed 2026-09-15  ->  eligible from 2026-09-29
#
# `tier` defaults to community; official is a designation Nous applies to its
# own repositories, not something an entry can claim.

name: hermesoffice-embed
repo: https://github.com/criptogus/hermesoffice-embed
sha: 0c19a7dc456e21cd93241b19a6f95a376e6bd75a
description: 'Open and edit .docx, .xlsx, .pptx and .pdf documents inside a Hermes pane, using the
  HermesOffice editor engines served over a local HTTP bridge. Adds a HermesOffice pane, a status-bar
  chip, a /hermesoffice page, a sidebar row and a palette command; documents open, edit and save as
  real Office files on disk.'
maintainer: criptogus
tier: community
requires_hermes: ">=0.21.0"
docs_url: https://github.com/criptogus/hermesoffice-embed
platforms:
  - macos
capabilities:
  provides_tools: []
  provides_hooks: []
  provides_middleware: []
  requires_env: []
